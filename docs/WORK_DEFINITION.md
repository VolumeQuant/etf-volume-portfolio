# 🏗️ ETF Pulse - 업무 정의서 (Work Definition)

**작성일**: 2024-12-04  
**버전**: 1.0

---

## 1. 업무 구조 개요

### 1.1 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                      사용자 (User)                       │
└────────────┬────────────────────────────────┬───────────┘
             │                                │
             ▼                                ▼
    ┌────────────────┐              ┌─────────────────┐
    │  웹 대시보드    │              │  텔레그램 봇     │
    │  (Frontend)    │              │  (Notification)  │
    └────────┬───────┘              └────────┬─────────┘
             │                                │
             ▼                                ▼
    ┌──────────────────────────────────────────────────┐
    │               FastAPI 서버 (Backend)              │
    │  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
    │  │ API 계층  │  │ 분석 계층 │  │ AI 서비스     │  │
    │  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │
    └───────┼─────────────┼────────────────┼──────────┘
            │             │                │
            ▼             ▼                ▼
    ┌──────────────────────────────────────────────────┐
    │              데이터 계층 (Data Layer)             │
    │  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │
    │  │ 수집기    │  │ 탐지기    │  │ DB           │  │
    │  └────┬─────┘  └────┬─────┘  └──────┬───────┘  │
    └───────┼─────────────┼────────────────┼──────────┘
            │             │                │
            ▼             ▼                ▼
    ┌──────────────────────────────────────────────────┐
    │              외부 데이터 소스 (External)          │
    │    yfinance    │   NewsAPI    │   Groq API       │
    └──────────────────────────────────────────────────┘
```

### 1.2 핵심 업무 도메인

| 업무 도메인 | 책임 | 우선순위 |
|-----------|------|---------|
| **WD-1: 데이터 수집** | ETF/뉴스 데이터 수집 | 🔴 필수 |
| **WD-2: 시그널 탐지** | 거래량 스파이크 탐지 | 🔴 필수 |
| **WD-3: 데이터 저장** | DB 저장 및 조회 | 🟡 중요 |
| **WD-4: API 서비스** | RESTful API 제공 | 🔴 필수 |
| **WD-5: 프론트엔드** | 사용자 인터페이스 | 🔴 필수 |
| **WD-6: AI 인사이트** | 뉴스 요약 및 분석 | 🟡 중요 |
| **WD-7: 알림 발송** | 텔레그램 실시간 알림 | 🟢 선택 |
| **WD-8: 배포 운영** | 프로덕션 배포 | 🔴 필수 |

---

## 2. 업무 정의: WD-1 데이터 수집 (Data Collection)

### 2.1 업무 목적
> ETF 거래량/가격 데이터와 금융 뉴스를 자동으로 수집하여, 분석 가능한 형태로 제공

### 2.2 책임 범위 (Responsibility)

#### In Scope ✅
- 26개 ETF의 OHLCV 데이터 수집 (yfinance)
- 금융 뉴스 RSS 피드 수집 (Yahoo Finance, Bloomberg)
- 데이터 검증 (결측치, 이상치 처리)
- 수집 실패 시 재시도 로직
- 수집 로그 기록

#### Out of Scope ❌
- 데이터 분석 (→ WD-2가 담당)
- 데이터 저장 (→ WD-3이 담당)
- 실시간 스트리밍 (일일 1회 수집)

### 2.3 입력/출력 (Input/Output)

```python
# 입력 (Input)
config = {
    "tickers": ["XLK", "XLF", ...],  # ETF 리스트
    "period": "1y",                   # 수집 기간
    "interval": "1d"                  # 데이터 간격
}

# 출력 (Output)
df = pd.DataFrame({
    "Ticker": ["XLK", "XLK", ...],
    "Date": [datetime, datetime, ...],
    "Open": [float, float, ...],
    "High": [float, float, ...],
    "Low": [float, float, ...],
    "Close": [float, float, ...],
    "Volume": [int, int, ...]
})
```

### 2.4 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-1.1** | yfinance API로 OHLCV 수집 | 🔴 필수 |
| **REQ-1.2** | 일일 자동 수집 (cron/scheduler) | 🟡 중요 |
| **REQ-1.3** | 수집 실패 시 재시도 (3회) | 🟡 중요 |
| **REQ-1.4** | 결측치 처리 (forward fill) | 🔴 필수 |
| **REQ-1.5** | 수집 시간 < 30초 (26개 ETF) | 🟡 중요 |
| **REQ-1.6** | RSS 뉴스 수집 (feedparser) | 🟢 선택 |
| **REQ-1.7** | Alpha Vantage 백업 API | 🟢 선택 |

### 2.5 성공 기준 (Acceptance Criteria)

```python
def test_data_collection():
    """데이터 수집 업무 검증"""
    collector = ETFDataCollector()
    
    # 1. 모든 ETF 수집 성공
    df = collector.fetch_multiple(SECTOR_ETFS.keys())
    assert len(df) > 0
    assert df['Ticker'].nunique() == 26
    
    # 2. 필수 컬럼 존재
    required_cols = ['Date', 'Open', 'High', 'Low', 'Close', 'Volume']
    assert all(col in df.columns for col in required_cols)
    
    # 3. 결측치 없음
    assert df[required_cols].isnull().sum().sum() == 0
    
    # 4. 수집 시간 < 30초
    assert collection_time < 30
```

### 2.6 에러 처리

| 에러 | 대응 |
|------|------|
| yfinance 차단 | Alpha Vantage로 폴백 |
| 네트워크 실패 | 3회 재시도 후 캐시 사용 |
| 데이터 없음 | 로그 기록 후 스킵 |
| 이상치 발견 | 통계적 필터링 (3σ) |

---

## 3. 업무 정의: WD-2 시그널 탐지 (Signal Detection)

### 3.1 업무 목적
> 거래량 데이터에서 이상 패턴(스파이크)을 탐지하고, 투자 시그널로 분류

### 3.2 핵심 알고리즘

#### 거래량 스파이크 정의
```python
# 1. 이동평균 계산
Volume_MA_20 = df.groupby('Ticker')['Volume'].rolling(20).mean()

# 2. 스파이크 비율
Spike_Ratio = Volume / Volume_MA_20

# 3. 이벤트 레벨 분류
if Spike_Ratio >= 2.5:
    Event_Level = "EXTREME"
elif Spike_Ratio >= 2.0:
    Event_Level = "HIGH"
elif Spike_Ratio >= 1.5:
    Event_Level = "MEDIUM"
elif Spike_Ratio >= 1.3:
    Event_Level = "ALERT"
else:
    Event_Level = None
```

### 3.3 입력/출력

```python
# 입력
df_raw = pd.DataFrame({
    "Ticker": [...],
    "Date": [...],
    "Volume": [...]
})

# 출력
df_signals = pd.DataFrame({
    "Ticker": ["XLK"],
    "Date": ["2024-12-04"],
    "Volume": [15000000],
    "Volume_MA": [6500000],
    "Spike_Ratio": [2.31],
    "Event_Level": ["EXTREME"],
    "Price_Change_Pct": [1.5]
})
```

### 3.4 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-2.1** | 20일 이동평균 계산 | 🔴 필수 |
| **REQ-2.2** | 4단계 이벤트 분류 | 🔴 필수 |
| **REQ-2.3** | 가격-거래량 상관관계 | 🟡 중요 |
| **REQ-2.4** | 섹터별 집계 | 🔴 필수 |
| **REQ-2.5** | 히스토리 비교 (과거 패턴) | 🟢 선택 |
| **REQ-2.6** | 백테스팅 (전략 검증) | 🟢 선택 |

### 3.5 섹터 집계 로직

```python
def aggregate_sectors(df_signals):
    """섹터별 거래량 스파이크 집계"""
    sector_summary = df_signals.groupby('Sector').agg({
        'Spike_Ratio': 'mean',        # 평균 스파이크
        'Event_Level': 'count',       # 이벤트 수
        'Price_Change_Pct': 'mean'    # 평균 가격 변화
    })
    
    # HOT/COLD 분류
    sector_summary['Status'] = sector_summary['Spike_Ratio'].apply(
        lambda x: 'HOT' if x >= 1.5 else 
                  'WARM' if x >= 1.2 else 
                  'NEUTRAL' if x >= 0.9 else 
                  'COLD'
    )
    
    return sector_summary
```

### 3.6 성공 기준

```python
def test_signal_detection():
    """시그널 탐지 업무 검증"""
    detector = VolumeEventDetector()
    
    # 1. 이동평균 계산
    df = detector.calculate_volume_features(df_raw)
    assert 'Volume_MA' in df.columns
    
    # 2. 스파이크 탐지
    events = detector.detect_events(df)
    assert all(events['Spike_Ratio'] >= 1.3)
    
    # 3. 이벤트 분류
    assert events['Event_Level'].isin(['EXTREME', 'HIGH', 'MEDIUM', 'ALERT']).all()
    
    # 4. 섹터 집계
    sectors = detector.aggregate_sectors(df)
    assert len(sectors) == 11
```

---

## 4. 업무 정의: WD-3 데이터 저장 (Data Storage)

### 4.1 업무 목적
> 수집/분석된 데이터를 영구 저장하고, 빠른 조회 제공

### 4.2 데이터베이스 스키마

#### 테이블 1: etf_daily (일일 데이터)
```sql
CREATE TABLE etf_daily (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ticker TEXT NOT NULL,
    date DATE NOT NULL,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    volume INTEGER,
    volume_ma_20 REAL,
    volume_spike_ratio REAL,
    event_level TEXT,  -- EXTREME, HIGH, MEDIUM, ALERT
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(ticker, date)
);

CREATE INDEX idx_ticker_date ON etf_daily(ticker, date);
CREATE INDEX idx_event_level ON etf_daily(event_level) WHERE event_level IS NOT NULL;
```

#### 테이블 2: news (뉴스)
```sql
CREATE TABLE news (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT NOT NULL,
    source TEXT,
    url TEXT UNIQUE,
    published_at TIMESTAMP,
    summary TEXT,           -- AI 생성 요약
    sectors TEXT,           -- JSON: ["XLK", "XLF"]
    sentiment TEXT,         -- POSITIVE, NEGATIVE, NEUTRAL
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_published_at ON news(published_at DESC);
CREATE INDEX idx_sectors ON news(sectors);
```

#### 테이블 3: alerts (알림 기록)
```sql
CREATE TABLE alerts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    alert_type TEXT,        -- VOLUME_SPIKE, NEWS_ALERT
    ticker TEXT,
    event_level TEXT,
    message TEXT,
    sent_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    channel TEXT,           -- telegram, email
    user_id TEXT
);
```

### 4.3 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-3.1** | SQLite 기본 구현 | 🔴 필수 |
| **REQ-3.2** | 일일 데이터 자동 저장 | 🔴 필수 |
| **REQ-3.3** | 히스토리 조회 API | 🔴 필수 |
| **REQ-3.4** | 중복 방지 (UNIQUE 제약) | 🔴 필수 |
| **REQ-3.5** | 인덱스 최적화 | 🟡 중요 |
| **REQ-3.6** | PostgreSQL 마이그레이션 | 🟢 선택 |
| **REQ-3.7** | 백업/복구 | 🟢 선택 |

### 4.4 저장 로직

```python
class DataStorage:
    def __init__(self, db_path="etf_pulse.db"):
        self.conn = sqlite3.connect(db_path)
        self._create_tables()
    
    def save_daily_data(self, df: pd.DataFrame):
        """일일 데이터 저장 (중복 시 UPDATE)"""
        df.to_sql(
            'etf_daily',
            self.conn,
            if_exists='append',
            index=False,
            method='multi'
        )
    
    def get_history(self, ticker: str, days: int = 365):
        """히스토리 조회"""
        query = f"""
        SELECT * FROM etf_daily
        WHERE ticker = ?
        AND date >= date('now', '-{days} days')
        ORDER BY date DESC
        """
        return pd.read_sql(query, self.conn, params=[ticker])
```

---

## 5. 업무 정의: WD-4 API 서비스 (API Service)

### 5.1 업무 목적
> 프론트엔드와 외부 클라이언트에게 RESTful API 제공

### 5.2 API 엔드포인트 정의

#### 5.2.1 분석 API

| 메서드 | 엔드포인트 | 설명 | 우선순위 |
|-------|----------|------|---------|
| GET | `/api/analysis/quick` | 빠른 스캔 (최근 5일) | 🔴 필수 |
| GET | `/api/analysis/full` | 전체 분석 (1년) | 🔴 필수 |
| GET | `/api/sectors` | 섹터 집계 | 🔴 필수 |
| GET | `/api/ticker/{ticker}` | 티커 상세 | 🔴 필수 |

#### 5.2.2 뉴스 API (Phase 2)

| 메서드 | 엔드포인트 | 설명 | 우선순위 |
|-------|----------|------|---------|
| GET | `/api/news/latest` | 최신 뉴스 | 🟡 중요 |
| GET | `/api/news/sector/{sector}` | 섹터별 뉴스 | 🟡 중요 |

#### 5.2.3 알림 API (Phase 3)

| 메서드 | 엔드포인트 | 설명 | 우선순위 |
|-------|----------|------|---------|
| POST | `/api/alerts/subscribe` | 알림 구독 | 🟢 선택 |
| GET | `/api/alerts/history` | 알림 기록 | 🟢 선택 |

### 5.3 API 응답 형식

#### 성공 응답
```json
{
  "success": true,
  "data": {
    "mode": "quick_scan",
    "timestamp": "2024-12-04T09:00:00Z",
    "data": [
      {
        "ticker": "XLK",
        "sector": "Technology",
        "date": "2024-12-04",
        "volume_spike_ratio": 2.31,
        "event_level": "EXTREME",
        "price_change_pct": 1.5
      }
    ]
  },
  "metadata": {
    "tickers_analyzed": 26,
    "execution_time_ms": 1250
  }
}
```

#### 에러 응답
```json
{
  "success": false,
  "error": {
    "code": "DATA_COLLECTION_FAILED",
    "message": "yfinance API 응답 없음",
    "details": "Connection timeout after 3 retries"
  },
  "timestamp": "2024-12-04T09:00:00Z"
}
```

### 5.4 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-4.1** | FastAPI 프레임워크 | 🔴 필수 |
| **REQ-4.2** | CORS 설정 | 🔴 필수 |
| **REQ-4.3** | 에러 핸들링 | 🔴 필수 |
| **REQ-4.4** | 응답 시간 < 3초 | 🟡 중요 |
| **REQ-4.5** | Swagger 문서 자동 생성 | 🟡 중요 |
| **REQ-4.6** | API 인증 (토큰) | 🟢 선택 |
| **REQ-4.7** | Rate Limiting | 🟢 선택 |

---

## 6. 업무 정의: WD-5 프론트엔드 (Frontend)

### 6.1 업무 목적
> 사용자에게 직관적이고 반응형인 웹 대시보드 제공

### 6.2 주요 화면 (Pages)

#### 6.2.1 홈 대시보드 (`/`)
```
┌─────────────────────────────────────────────┐
│  ETF Pulse                        [ Dark 🌙] │
├─────────────────────────────────────────────┤
│  📊 섹터 히트맵 (11개 섹터)                   │
│  ┌─────┬─────┬─────┐                        │
│  │ XLK │ XLF │ XLE │  HOT/COLD 색상         │
│  │ 🔴  │ 🔵  │ 🟢  │                        │
│  └─────┴─────┴─────┘                        │
├─────────────────────────────────────────────┤
│  🔥 최근 이벤트 (Top 10)                      │
│  ┌─────────────────────────────────────────┐│
│  │ XLK │ 2024-12-04 │ EXTREME │ 2.31x     ││
│  │ XLF │ 2024-12-03 │ HIGH    │ 2.05x     ││
│  └─────────────────────────────────────────┘│
├─────────────────────────────────────────────┤
│  📈 빠른 스캔 (6개 주요 ETF)                  │
└─────────────────────────────────────────────┘
```

#### 6.2.2 티커 상세 (`/ticker/:symbol`)
```
┌─────────────────────────────────────────────┐
│  ← Back      XLK (Technology)               │
├─────────────────────────────────────────────┤
│  📊 가격 차트 (1년)                           │
│  ┌─────────────────────────────────────────┐│
│  │        /\  /\                            ││
│  │       /  \/  \  /\                       ││
│  └─────────────────────────────────────────┘│
├─────────────────────────────────────────────┤
│  📊 거래량 차트 + 이동평균                     │
│  ┌─────────────────────────────────────────┐│
│  │  ███   ██  ████  ██   ← Volume          ││
│  │  ────────────────────  ← MA_20          ││
│  └─────────────────────────────────────────┘│
├─────────────────────────────────────────────┤
│  🔔 이벤트 히스토리                           │
└─────────────────────────────────────────────┘
```

### 6.3 핵심 컴포넌트

| 컴포넌트 | 책임 | 기술 |
|---------|------|------|
| `SectorHeatmap` | 11개 섹터 히트맵 | Recharts |
| `VolumeChart` | 거래량 차트 | Recharts |
| `PriceChart` | 가격 차트 | Recharts |
| `EventList` | 이벤트 목록 | React Table |
| `TickerCard` | ETF 카드 | Tailwind CSS |
| `LoadingSpinner` | 로딩 상태 | Framer Motion |

### 6.4 상태 관리 (Zustand)

```typescript
interface ETFStore {
  // 상태
  etfs: ETF[];
  sectors: Sector[];
  loading: boolean;
  error: string | null;
  
  // 액션
  fetchQuickScan: () => Promise<void>;
  fetchFullAnalysis: () => Promise<void>;
  fetchTickerDetail: (ticker: string) => Promise<void>;
  setError: (error: string) => void;
}
```

### 6.5 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-5.1** | React 18 + TypeScript | 🔴 필수 |
| **REQ-5.2** | 섹터 히트맵 | 🔴 필수 |
| **REQ-5.3** | 거래량/가격 차트 | 🔴 필수 |
| **REQ-5.4** | 반응형 디자인 | 🟡 중요 |
| **REQ-5.5** | 다크모드 | 🟡 중요 |
| **REQ-5.6** | 로딩/에러 상태 | 🔴 필수 |
| **REQ-5.7** | 애니메이션 | 🟢 선택 |

---

## 7. 업무 정의: WD-6 AI 인사이트 (AI Service)

### 7.1 업무 목적
> 거래량 스파이크와 뉴스를 결합하여, "왜 이 섹터인가" 설명

### 7.2 입력/출력

```python
# 입력
context = {
    "ticker": "XLK",
    "sector": "Technology",
    "spike_ratio": 2.31,
    "price_change": 1.5,
    "date": "2024-12-04",
    "news": [
        {"title": "NVDA Q3 earnings beat expectations", ...},
        {"title": "Semiconductor sector surges", ...}
    ]
}

# 출력
insight = {
    "summary": "기술 섹터 거래량 2.3배 스파이크. 엔비디아 실적 호조로 반도체 전반 상승.",
    "key_drivers": ["엔비디아 실적", "AI 수요 증가"],
    "sentiment": "POSITIVE",
    "confidence": 0.85
}
```

### 7.3 프롬프트 템플릿

```python
PROMPT_TEMPLATE = """
당신은 금융 데이터 분석가입니다.

## 데이터
- 티커: {ticker}
- 섹터: {sector}
- 거래량 스파이크: {spike_ratio}배
- 가격 변화: {price_change}%
- 날짜: {date}

## 관련 뉴스
{news_list}

## 태스크
1. 왜 이 섹터에 거래량 스파이크가 발생했는지 3줄로 요약
2. 주요 원인 키워드 3개 추출
3. 시장 심리 (POSITIVE/NEGATIVE/NEUTRAL)

## 제약
- 투자 추천 절대 금지
- 사실 기반 분석만
- 3줄 이내
"""
```

### 7.4 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-6.1** | Groq API 연동 | 🔴 필수 |
| **REQ-6.2** | 프롬프트 최적화 | 🔴 필수 |
| **REQ-6.3** | Rule-based 폴백 | 🟡 중요 |
| **REQ-6.4** | 응답 시간 < 5초 | 🟡 중요 |
| **REQ-6.5** | 뉴스-거래량 연관 분석 | 🟢 선택 |
| **REQ-6.6** | 감성 분석 | 🟢 선택 |

---

## 8. 업무 정의: WD-7 알림 발송 (Notification)

### 8.1 업무 목적
> EXTREME 이벤트 발생 시 사용자에게 실시간 알림

### 8.2 알림 트리거

```python
def should_send_alert(event: dict) -> bool:
    """알림 발송 조건"""
    return (
        event['event_level'] == 'EXTREME' and
        event['spike_ratio'] >= 2.5 and
        not is_duplicate_within_24h(event)  # 중복 방지
    )
```

### 8.3 메시지 템플릿

```
🔥 ETF 거래량 EXTREME 스파이크!

티커: XLK (Technology)
날짜: 2024-12-04
거래량: 2.31배
가격 변화: +1.5%

AI 요약:
엔비디아 실적 호조로 반도체 섹터 전반 상승.
기술주로 자금 이동 확인.

🔗 상세 보기: https://etf-pulse.vercel.app/ticker/XLK
```

### 8.4 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-7.1** | 텔레그램 봇 구현 | 🟡 중요 |
| **REQ-7.2** | 구독 관리 (subscribe/unsubscribe) | 🟡 중요 |
| **REQ-7.3** | 중복 알림 방지 (24시간) | 🔴 필수 |
| **REQ-7.4** | 알림 기록 저장 | 🟢 선택 |
| **REQ-7.5** | 이메일 알림 (선택) | 🟢 선택 |

---

## 9. 업무 정의: WD-8 배포 운영 (Deployment & Operations)

### 9.1 업무 목적
> 프로덕션 환경에 안정적으로 배포하고 모니터링

### 9.2 배포 아키텍처

```
┌─────────────────────────────────────────────┐
│  Vercel (Frontend)                           │
│  - React 빌드 배포                            │
│  - CDN 자동 캐싱                              │
│  - https://etf-pulse.vercel.app              │
└────────────────┬────────────────────────────┘
                 │ API 호출
                 ▼
┌─────────────────────────────────────────────┐
│  Railway (Backend)                           │
│  - FastAPI 서버                               │
│  - PostgreSQL DB                             │
│  - Cron 스케줄러                              │
│  - https://api.etf-pulse.com                 │
└─────────────────────────────────────────────┘
```

### 9.3 배포 체크리스트

#### 프론트엔드 (Vercel)
- [ ] 빌드 최적화 (`npm run build`)
- [ ] 환경변수 설정 (`VITE_API_URL`)
- [ ] CORS 설정
- [ ] 도메인 연결 (선택)

#### 백엔드 (Railway)
- [ ] Dockerfile 작성
- [ ] 환경변수 설정 (`GROQ_API_KEY`)
- [ ] DB 마이그레이션
- [ ] Health Check 엔드포인트 (`/health`)
- [ ] 로그 모니터링

### 9.4 구현 요구사항

| 요구사항 | 설명 | 우선순위 |
|---------|------|---------|
| **REQ-8.1** | Vercel 배포 | 🔴 필수 |
| **REQ-8.2** | Railway 배포 | 🔴 필수 |
| **REQ-8.3** | HTTPS 설정 | 🔴 필수 |
| **REQ-8.4** | CI/CD (GitHub Actions) | 🟡 중요 |
| **REQ-8.5** | 에러 모니터링 (Sentry) | 🟢 선택 |
| **REQ-8.6** | 로그 집계 | 🟢 선택 |

---

## 10. 업무 간 인터페이스 (Interfaces)

### 10.1 업무 흐름

```
[WD-1 데이터 수집]
        ↓ (DataFrame)
[WD-2 시그널 탐지]
        ↓ (Events)
[WD-3 데이터 저장] ← ─ ─ ─ ─ ┐
        ↓                     │
[WD-4 API 서비스]             │
        ↓                     │
[WD-5 프론트엔드]             │
                              │
[WD-6 AI 인사이트] ← ─ ─ ─ ─ ┘
        ↓
[WD-7 알림 발송]
```

### 10.2 데이터 계약 (Data Contract)

#### Contract 1: WD-1 → WD-2
```python
# pandas DataFrame
columns = ['Ticker', 'Date', 'Open', 'High', 'Low', 'Close', 'Volume']
dtypes = {'Volume': int, 'Close': float, ...}
constraints = {
    'Volume': lambda x: x > 0,
    'Date': lambda x: x >= '2024-01-01'
}
```

#### Contract 2: WD-2 → WD-4
```python
# Events DataFrame
columns = ['Ticker', 'Date', 'Volume_Spike_Ratio', 'Event_Level']
event_levels = ['EXTREME', 'HIGH', 'MEDIUM', 'ALERT']
```

#### Contract 3: WD-4 → WD-5
```json
// REST API Response
{
  "data": [...],
  "metadata": {...},
  "timestamp": "ISO-8601"
}
```

---

## 11. 업무 우선순위 로드맵

### Phase 1 (Week 1-4): 핵심 기능
```
✅ WD-1: 데이터 수집 (완료)
✅ WD-2: 시그널 탐지 (완료)
✅ WD-4: API 서비스 (기본)
🚧 WD-5: 프론트엔드 (진행 중)
   - 섹터 히트맵
   - 거래량 차트
   - 대시보드 통합
```

### Phase 2 (Week 5-8): 고급 기능
```
⬜ WD-3: 데이터 저장 (SQLite)
⬜ WD-6: AI 인사이트 (뉴스 요약)
⬜ WD-7: 알림 발송 (텔레그램)
```

### Phase 3 (Week 9-14): 폴리싱 & 배포
```
⬜ WD-5: UI/UX 개선
⬜ WD-8: 배포 운영
⬜ 문서화 & 데모
```

---

## 12. 핵심 지표 (KPIs)

### 12.1 기술 지표

| 지표 | 목표 | 현재 |
|------|------|------|
| API 응답 시간 | < 3초 | - |
| 데이터 수집 시간 | < 30초 | - |
| 프론트엔드 로딩 | < 3초 | - |
| AI 응답 시간 | < 5초 | - |
| 에러율 | < 1% | - |

### 12.2 비즈니스 지표

| 지표 | 목표 | 현재 |
|------|------|------|
| 일일 활성 사용자 | 본인 | - |
| 알림 구독자 | 5명 | - |
| 데모 완료 | 1회 | - |
| GitHub 스타 | 10개 | - |

---

## 13. 위험 관리 (Risk Management)

### 13.1 기술 리스크

| 리스크 | 영향 | 확률 | 대응 |
|--------|------|------|------|
| yfinance 차단 | 높음 | 중간 | Alpha Vantage 백업 |
| Groq API 비용 초과 | 중간 | 낮음 | Rule-based 폴백 |
| React 학습 곡선 | 중간 | 높음 | 튜토리얼 + 샘플 코드 |
| 배포 실패 | 낮음 | 낮음 | 로컬 데모 준비 |

### 13.2 일정 리스크

| 리스크 | 대응 |
|--------|------|
| 시간 부족 | MVP 범위 축소 (알림 제외) |
| 예상보다 복잡 | Phase 2 기능 선택적 구현 |
| 버그 다수 | 테스트 코드 작성 |

---

## 14. 성공 기준 (Definition of Done)

### 각 업무의 완료 조건

#### WD-1: 데이터 수집 ✅
- [ ] 26개 ETF 데이터 수집 성공
- [ ] 결측치 0개
- [ ] 수집 시간 < 30초
- [ ] 에러 로깅 완료

#### WD-2: 시그널 탐지 ✅
- [ ] 4단계 이벤트 분류
- [ ] 섹터 집계 정확도 100%
- [ ] 단위 테스트 통과

#### WD-5: 프론트엔드
- [ ] 섹터 히트맵 작동
- [ ] 반응형 디자인
- [ ] 로딩/에러 상태 처리
- [ ] 타입 에러 0개

#### WD-8: 배포
- [ ] Vercel 배포 성공
- [ ] Railway 배포 성공
- [ ] HTTPS 설정 완료
- [ ] 실제 URL 생성

---

## 15. 요약

### 핵심 업무 8개
```
1. WD-1: 데이터 수집 → yfinance로 ETF 데이터
2. WD-2: 시그널 탐지 → 거래량 스파이크 탐지
3. WD-3: 데이터 저장 → SQLite 영구 저장
4. WD-4: API 서비스 → FastAPI RESTful
5. WD-5: 프론트엔드 → React 대시보드
6. WD-6: AI 인사이트 → Groq LLM 요약
7. WD-7: 알림 발송 → 텔레그램 봇
8. WD-8: 배포 운영 → Vercel + Railway
```

### 업무 의존성
```
WD-1 (수집) → WD-2 (탐지) → WD-3 (저장)
                              ↓
WD-6 (AI) ← ─ ─ ─ ─ WD-4 (API) → WD-5 (FE)
   ↓
WD-7 (알림)

WD-8 (배포): 모든 업무 완료 후
```

### 현재 진행률
```
✅ WD-1: 100%
✅ WD-2: 100%
⬜ WD-3: 0%
🚧 WD-4: 70%
🚧 WD-5: 40%
⬜ WD-6: 0%
⬜ WD-7: 0%
⬜ WD-8: 0%

전체: ~35% 완료
```

---

**이 문서는 모든 구현의 청사진입니다.**

*작성: 2024-12-04*
